#include <stdio.h>
#define A 10

/**
*main - shows simple implementation of a macro in c
*A is the macro name
*10 is the character sequence
*/

int main()
{
   
    int x = A;
    
    printf("Output: %d", x);

    return 0;
}
